# URL of the Devika core backend (HuggingFace Space or wherever devika.py runs).
# Read at build time by src/lib/api.js via import.meta.env.VITE_API_BASE_URL.
# Must include protocol and NO trailing slash.
VITE_API_BASE_URL=https://octoborg-devika-core.hf.space
